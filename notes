TF入门
tf计算模型——计算图
tensor+flow 张量（简单理解为多维数组），流动（张量之间通过计算相互转换）
tf每一个计算都是计算图上的一个节点，节点之间的边描述了计算之间的依赖关系。
一般使用默认计算图，也可生成新的计算图，不同计算图上的张量和计算都不会共享。（隔离）

计算图可以通过tf.Graph.device函数来指定运行计算的设备  with g.device('/gpu:0'):…………使用gpu

在一个计算图中，可以通过集合（collection)来管理不同类别的资源（张量/变量/队列资源等等）

tf数据模型——张量
所有数据通过其表示，简单理解为多维数组，但与数组有所不同。
零阶张量——标量，也就是一个数；
一阶张量为向量，也就是一维数组；
n阶张量——n维数组。

张量主要保存三个属性：名字（name），维度（shape）和类型（type）
name:唯一标示符，并给出如何计算出来的
张量命名形式“node:src_output” 分别表示节点名称：当前张量来自节点的第几个输出。
如add：0表示节点“add”输出的第一个结果

shape:维度信息
type：张量类型
类型不匹配时会报错，默认类型为，不带小数点的数为int32，带小数点的默认为float32
tf支持的14种类型：
实数(tf.float32,tf.float64)
整数(tf.int8,tf.int16,tf.int32,tf.int64,tf.uint8)
布尔型(tf.bool)
复数(tf.complex64,tf.complex128)

tf运行模型——会话(Session)
